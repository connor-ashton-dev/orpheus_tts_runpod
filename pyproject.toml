[project]
name = "runpod-tts"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "orpheus-speech>=0.1.0",
    "runpod>=1.7.9",
    "wave>=0.0.2",
]
